interface Customer {
  id: string;
  firstName: string;
  lastName: string;
  notes?: string;
  vatId?: string;
  addressAddition?: string;
  streetAndNumber?: string;
  postalCode?: string;
  city?: string;
  country?: string;
}
